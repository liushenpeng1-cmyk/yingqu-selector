# ──────────────────────────────────────────────────────────────
# 引流合作 · 合作方申请平台
# ──────────────────────────────────────────────────────────────

# 1. 合作方基地址
# 用户点击「申请 →」或「批量申请 N 个 →」时跳转到这里。
#
# Simple 模式:  直接跳到合作方官网首页即可,如 https://ioffer.ai
# Full 模式:   跳到合作方专用的接收页,如 https://ioffer.ai/apply
#
# 未设置时使用占位 https://partner.example.com/apply。
NEXT_PUBLIC_PARTNER_URL=https://partner.example.com/apply

# 2. 跳转模式(可选,默认 simple)
#
# simple — 只跳转 PARTNER_BASE_URL + ref=yingqu&utm_source=yingqu 归因参数。
#          合作方还在开发 /apply 接收端时用这个,先让按钮可点击引流,
#          用户到对方官网自行注册/填表单,依然能通过 ref 归因分成。
#
# full   — 发送完整筛选 payload(GPA、语言、学校/专业 id、地区等)。
#          合作方 /apply 端就绪、联调通过后翻到这个值,无需改代码。
#
# 参数编码约定见 src/lib/referral.ts 顶部注释 + docs/partner-integration-spec.md。
NEXT_PUBLIC_PARTNER_MODE=simple
